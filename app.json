{
  "expo": {
    "name": "FormBase",
    "slug": "FormBase",
    "version": "1.0.0",
    "orientation": "portrait",
    "icon": "./assets/icon.png",
    "userInterfaceStyle": "light",
    "newArchEnabled": true,
    "splash": {
      "image": "./assets/splash-icon.png",
      "resizeMode": "contain",
      "backgroundColor": "#ffffff"
    },
    "plugins": [
      [
        "expo-camera",
        {
          "cameraPermission": "Allow FormBase to access your camera to capture images for form fields."
        }
      ],
      [
        "expo-image-picker",
        {
          "photosPermission": "Allow FormBase to access your photo library to select images for form fields."
        }
      ],
      [
        "expo-location",
        {
          "locationAlwaysAndWhenInUsePermission": "Allow FormBase to access your location to capture GPS coordinates for form fields."
        }
      ]
    ],
    "ios": {
      "supportsTablet": true,
      "bundleIdentifier": "com.uq.formbase",
      "infoPlist": {
        "NSCameraUsageDescription": "Allow FormBase to access your camera to capture images for form fields.",
        "NSPhotoLibraryUsageDescription": "Allow FormBase to access your photo library to select images for form fields.",
        "NSLocationWhenInUseUsageDescription": "Allow FormBase to access your location to capture GPS coordinates for form fields."
      }
    },
    "android": {
      "adaptiveIcon": {
        "foregroundImage": "./assets/adaptive-icon.png",
        "backgroundColor": "#ffffff"
      },
      "package": "com.uq.formbase",
      "permissions": [
        "CAMERA",
        "READ_EXTERNAL_STORAGE",
        "WRITE_EXTERNAL_STORAGE",
        "ACCESS_FINE_LOCATION",
        "ACCESS_COARSE_LOCATION"
      ],
      "edgeToEdgeEnabled": true,
      "predictiveBackGestureEnabled": false,
      "config": {
        "googleMaps": {
          "apiKey": "YOUR_GOOGLE_MAPS_API_KEY"
        }
      }
    },
    "web": {
      "favicon": "./assets/favicon.png"
    }
  }
}
